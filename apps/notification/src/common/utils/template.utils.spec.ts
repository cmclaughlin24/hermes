describe('template.utils.ts', () => {
  describe('compileTextTemplate()', () => {
    it.todo('should yield a string (no pattern)');

    it.todo('should yield a string (context is null/undefined)');

    it.todo('should yield a string ()');

    it.todo('should throw an error if the context is not an object');

    it.todo('should throw an error if the context is an array');
  });
});

describe('notification-job.utils.ts', () => {
  describe('createNotificationJob()', () => {});

  describe('hasDeliveryMethods()', () => {
    it('should yield true if the subscription member has at least one delivery method defined in the distribution rule', () => {
      // Arrange.
      // Act.
      // Assert.
    });

    it('should yield false otherwise', () => {
      // Arrange.
      // Act.
      // Assert.
    });
  });

  describe('hasDeliveryWindow()', () => {});

  describe('isBetweenTimes()', () => {
    it('should yield true if the time is equal to the start time', () => {
      // Arrange.
      // Act.
      // Assert.
    });

    it('should yield true if the time is greater than the start time and less then the end time', () => {
      // Arrange.
      // Act.
      // Assert.
    });

    it('should yield true if the time is equal to the end time', () => {
      // Arrange.
      // Act.
      // Assert.
    });

    it('should yield false if the time is less than the start time', () => {
      // Arrange.
      // Act.
      // Assert.
    });

    it('should yield false if the time is greater than the end time', () => {
      // Arrange.
      // Act.
      // Assert.
    });
  });

  describe('reduceToDeliveryMethodsMap()', () => {});

  describe('mapToNotificationJobs()', () => {});
});

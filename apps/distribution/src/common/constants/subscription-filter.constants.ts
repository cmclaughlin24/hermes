export enum SubscriptionFilterOps {
  EQUALS = 'equals',
  NEQUALS = 'nequals',
  OR = 'or',
  MATCHES = 'matches',
}

export enum SubscriptionFilterJoinOps {
  AND = 'and',
  OR = 'or',
  NOT = 'not',
}

{
  "name": "npm-package-version",
  "version": "1.0.0",
  "description": "A Github Action that outputs the version number from the package.json file.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wm. Curtis McLaughlin",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1"
  }
}
